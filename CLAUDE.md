# CLAUDE.md

버티컬 배드민턴 매칭 시스템 — 구글 시트/Apps Script와 연동되는 서버리스 웹 앱.

## 파일 구조

- `index.html` — UI 마크업 (모달, 헤더, 코트/매칭/대기 패널)
- `styles.css` — 디자인·레이아웃 (다크 테마, 반응형)
- `app.js` — 비즈니스 로직 (매칭 엔진, 시트 연동, 상태 관리, `CONFIG` 객체 포함)
- `apps-script.gs` — 구글 Apps Script 서버 코드. **`app.js`/`index.html`과 별도로 버전 관리되며, 구글 시트 Apps Script 편집기에 수동으로 붙여넣고 배포해야 반영됨.**

## 필수 규칙

### 1. Apps Script ↔ 프론트엔드 동기화

`app.js`에서 상태 저장/조회 로직(`saveState`/`clearState`/`getState` 호출부, 참가자·게임기록·출석 데이터 구조)을 변경할 때마다:

- `apps-script.gs`의 대응 함수(`doGet`/`doPost`, `onEdit` 등)도 함께 검토하고 필요하면 수정한다.
- 구글 시트 "상태" 탭에 저장되는 JSON 구조가 바뀌는 변경이면, `app.js`의 직렬화/역직렬화 코드와 `apps-script.gs` 양쪽에서 형식이 일치하는지 반드시 확인하고 맞춘다.
- 프론트엔드만 고치고 `apps-script.gs`를 그대로 두면 실제 배포본과 어긋나므로, 소스만 수정하고 끝내지 않는다 — 로컬 `.gs` 파일과 실제 시트에 배포된 스크립트 양쪽을 함께 고려한다.

### 2. UI / 아이콘

- 현재 다크 테마 및 색상 체계(`styles.css`의 CSS 변수)를 유지한다. 임의로 배색을 바꾸지 않는다.
- 아이콘은 [Lucide](https://lucide.dev) (`data-lucide="..."` + `lucide.createIcons()`)만 사용한다.
- 이모지를 UI 텍스트/아이콘 대체용으로 사용하지 않는다 (README 등 문서 파일은 예외).

### 3. 모서리(border-radius)

- 버튼/레이아웃 모서리는 기존 `--r`(5px), `--r-lg`(8px) 변수를 그대로 사용한다.
- 새로운 요소에 과도하게 둥근 값(예: `border-radius: 12px+`, `9999px`, `50%`인 pill 형태 버튼 등)을 임의로 추가하지 않는다.

### 4. 수정 완료 후 반영 체크리스트

코드 수정을 마칠 때마다, 다음을 **순서대로** 정리해서 사용자에게 안내한다:

1. `apps-script.gs` 변경 여부 및 수정 내용
2. 구글 시트 Apps Script 편집기에 붙여넣기 필요 여부
3. Apps Script 재배포 필요 여부 — "새 배포"가 아니라 기존 배포에서 "버전 관리 → 새 버전"으로 업데이트 (배포 URL 유지)
4. 구글 시트 "상태" 탭 JSON 양식 변경 여부 및 마이그레이션 필요 여부 (기존 저장된 상태와 호환되는지)
5. `app.js` `CONFIG` 값 변경/추가 여부 (예: `SHEET_ID`, `APPS_SCRIPT_URL`, 탭 이름 등 사용자가 직접 채워야 하는 값)
6. 브라우저에서 확인이 필요한 UI 변경사항 요약
